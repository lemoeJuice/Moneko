# Moneko Feature Guide

## 产品定位

Moneko 是一个个人日常支出记录工具，不是完整的财务管理系统。它的核心目标是：

1. 在几秒钟内完成一笔消费记录。
2. 排除偶发的一次性支出后，观察每日生活成本和主要消费类别。

应用不包含收入、账户余额、预算、投资、OCR、AI、登录、后端或云同步。

## 核心功能

### 快速记录

- 首页为“记录”页，默认显示当前业务日的流水。
- 右下角悬浮 `+` 打开新记录窗口。
- 输入金额后点击分类即可保存，不需要额外确认。
- 默认分类：正餐、零食、交通、购物、日用、娱乐、其他。
- 一次性支出复选框位于金额输入旁边。
- 备注可选，不会阻塞快速记账。
- 可以在低频的日期/时间设置中修改记录时间。
- 新增后提供短暂的撤销操作。

### 流水管理

- 查看某个业务日的全部记录。
- 点击已有记录进入编辑窗口。
- 编辑金额、分类、备注、一次性状态和真实日期/时间。
- 支持删除记录。
- 日期浏览不会另开复杂的流水页面。

### 业务日

Moneko 不以自然日午夜作为一天的边界。每个业务日定义为：

```text
当天 04:00:00 inclusive ～ 第二天 04:00:00 exclusive
```

例如凌晨 02:30 发生的消费，归入前一天的记录。这个规则统一用于首页流水、统计图表、周期判断和 CSV 日期。

编辑窗口仍显示记录的真实日历日期和时间；新记录在 04:00 前输入时，会自动按业务日转换到正确的实际日期。

## 统计逻辑

### 周期

- 统计页默认以设置中的周期起始日开始计算一个完整周期。
- 默认起始日为每月 1 日。
- 可以在设置中选择每月 1 至 28 日作为周期起点。
- 统计图表上方保留开始日期和结束日期选择框。
- 日期框旁的左右按钮可以切换上一个或下一个周期。
- 手动修改日期后，会进入自定义范围模式。

### 第一笔记录与当前时间

- 图表仍按完整的一个月或当前选定周期绘制。
- 如果周期开始到第一笔记录之间没有记录，汇总数据实际从第一笔记录开始计算。
- 周期结束晚于当前业务日时，数据截止到当前业务日；未来日期不会产生数据。
- 总计金额包含今天的记录。
- 如果正在统计当前周期，平均值的自然日分母只计算到昨天，因此不会把今天计入平均天数，但今天的金额仍在总计中。
- 过去已经结束的周期，平均值使用完整实际周期天数。

### 日常支出图表

- 每天对应一根柱子。
- 柱子按分类堆叠，分类颜色在全应用中固定。
- 一次性支出不进入日常支出柱状图。
- 点击柱子可以查看当天日常总额和各分类金额。
- 图表会根据容器宽度自适应，不依赖横向滚动。
- 图表指标中并列展示日常支出和一次性金额，并提供周期切换按钮。

### 平均值

下方统计展示：

- 平均每日生活成本。
- 各日常类别的日均金额。
- 各类别周期累计金额。
- 日常支出为 0 的类别会隐藏，不显示空项目。

一次性支出在独立区域显示，包含周期总额、日期、分类、备注和金额。

## 设置与备份

设置页提供：

- 统计周期默认起始日。
- 完整 JSON 备份导出。
- JSON 备份恢复。
- CSV 导出。
- 离线优先说明。
- 版本信息和本地检查更新入口。

JSON 备份包含 `version`、导出时间和完整 `Expense` 记录。导入前会校验基本结构、金额整数、分类、时间字段和重复 ID；已有数据时不会静默覆盖。

## PWA 与离线能力

- Service Worker 由 vite-plugin-pwa 生成。
- JavaScript、CSS、HTML、图标和 Workbox 资源随构建产物发布。
- 不使用 CDN、远程字体或运行时远程 API。
- 首次加载并缓存完成后，新增、编辑、删除、历史查看、周期统计和备份功能可以离线使用。
- 数据仍然保存在当前设备和当前浏览器中，离线缓存不等于云备份。

## 自部署

### GitHub Pages

仓库已包含 `.github/workflows/deploy.yml`，适用于 GitHub Pages 的 Actions 部署方式。

1. 将代码推送到 GitHub 仓库的 `main` 或 `master` 分支。
2. 在仓库 `Settings` → `Pages` 中将 Source 设置为 `GitHub Actions`。
3. 确认仓库允许 Actions 写入 Pages：`Settings` → `Actions` → `General` 中保持 Actions 可运行；如仓库要求，允许 workflow 使用读写权限。
4. 等待 `Deploy to GitHub Pages` workflow 完成。
5. 在 Pages 设置或 workflow 的 environment 中打开发布地址。

Vite 配置会读取 `GITHUB_REPOSITORY`，自动把 base 设置为 `/<repository-name>/`。不要把仓库名手动写死到源码中。

### 任意静态服务器

```bash
npm ci
npm run build
```

将生成的 `dist/` 目录作为静态网站根目录即可。应用没有客户端路由，不需要配置 history fallback。静态服务器只负责资源访问，账目仍保存在用户浏览器本地。

## 项目结构

```text
src/
  components/       可复用界面组件
  constants/        固定分类和颜色
  data/             IndexedDB Repository
  stores/           Pinia 数据与设置状态
  utils/            日期、货币、周期和备份逻辑
  views/            记录、统计、设置页面
```

业务组件通过 Repository 和 Pinia 访问账目，不直接操作 IndexedDB，为未来可能加入同步能力保留了清晰边界，但当前版本不实现同步。
